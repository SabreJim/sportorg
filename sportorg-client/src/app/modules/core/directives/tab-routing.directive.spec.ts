import { TabRoutingDirective } from './tab-routing.directive';

describe('TabRoutingDirective', () => {
  it('should create an instance', () => {
    const directive = new TabRoutingDirective();
    expect(directive).toBeTruthy();
  });
});
